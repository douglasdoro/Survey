<%= simple_form_for @survey do |f| %>
  <%= f.input :name %>
  <%= f.input :description, :input_html => { :rows => 4} %>
	<%= f.simple_fields_for :questions do |builder| %>
		<%= render 'question_fields', :f => builder %>
	<% end %>
  <%= f.submit "Criar Pesquisa" %>
<% end %>
